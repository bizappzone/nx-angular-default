import { Route } from '@angular/router';
import { HomePageComponent } from './home-page/home-page.component'; // Adjust the import path as necessary

export const appRoutes: Route[] = [{ path: '', component: HomePageComponent }];
